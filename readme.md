# vpc module

This module is to developed for joindevops company, Projects inside join devops follow this module to create their VPC

## input 
*Project (required) - users should specify their project name
*environment (required) - users should specify thier environment
*cidr_block(optional) - users can provide their CIDR block. Defaule is 10.0.0.0/16

